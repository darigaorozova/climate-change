Это отличная мысль. Хорошая документация — залог того, что проект "взлетит" на чужой машине без нервотрепки.

Я составил подробный `README.md`, сфокусированный именно на **настройке окружения под Windows**, так как это было самым сложным местом.

Создай файл `README.md` в корне проекта и вставь туда этот текст:

***

```markdown
# 🌍 Курсовой проект: Big Data & Climate Change

Проект реализует полный ETL-пайплайн для анализа климатических данных ERA5, их загрузки в ClickHouse по схеме "Звезда" и предсказания температуры с помощью ML (PySpark + Scikit-Learn).

## 📂 Структура проекта

```text
project_root/
├── ETL/
│   ├── controller.py          # Главный оркестратор (запускать его!)
│   ├── daily_ingest.py        # Скачивание данных (API -> Parquet)
│   └── process_data_spark.py  # Обработка Spark (Parquet -> ClickHouse)
├── ML/
│   ├── data_loader.py         # Выгрузка из БД для обучения
│   ├── train_model.py         # Обучение модели
│   └── visualize.py           # Графики и тесты
├── warehouse/
│   ├── config.py              # Настройки БД (хост, порт, юзер)
│   ├── connection.py          # Методы подключения
│   └── create_tables.py       # DDL скрипт (создание таблиц)
├── raw_data/                  # Сюда качаются файлы (создается автоматически)
└── pipeline.log               # Логи работы
```

---

## 🛠 Предварительная настройка (Важно!)

Для запуска на Windows нужно установить несколько компонентов.

### 1. Java и Hadoop и Apache Spark (Есть видел я скину!)
PySpark требует **Java 17** (не 21 и не 8) и **winutils**.

1.  Установи **JDK 17** (например, Eclipse Adoptium).
2.  Скачай `winutils.exe` для Hadoop 3.2.
3.  Создай папку `C:\hadoop\bin` и положи туда `winutils.exe`.

### 2. Ключ API (CDS Copernicus) (Этот файл есть у меня скину!)
1.  Зарегистрируйся на [CDS Copernicus](https://cds.climate.copernicus.eu/).
2.  Создай файл `.cdsapirc` в папке пользователя (`C:\Users\TvoyUser\.cdsapirc`).
3.  Вставь туда url и ключ:
    ```text
    url: https://cds.climate.copernicus.eu/api/v2
    key: {UID}:{API_KEY}
    ```

### 3. ClickHouse
База данных должна быть запущена (Docker или Cloud).
*   **Порт:** 9000 (нативный TCP).
*   **Конфиг:** Проверь настройки в `warehouse/config.py`.


---

## 🚀 Установка зависимостей

В корне проекта выполни:
```bash
pip install -r requirements.txt
```
*(Если файла нет, см. список библиотек ниже)*.

---

## ▶️ Запуск

### 1. Создание таблиц в БД
Запусти один раз, чтобы создать структуру (Star Schema):
```bash
python -m warehouse.create_tables
```

### 2. Запуск ETL (Ежедневный процесс)
Этот скрипт скачает данные, обработает их в Spark и зальет в ClickHouse:
```bash
python ETL/controller.py
```
*Логи выполнения смотри в файле `pipeline.log`.*

### 3. Запуск ML (Обучение и Тест)
Обучение модели на данных из ClickHouse:
```bash
python -m ML.train_model
```
Создание графиков с результатами:
```bash
python -m ML.visualize
```

---

## ⚠️ Частые проблемы

1.  **UnicodeDecodeError / Кодировка**:
    *   В Windows консоль может выдавать ошибки на русских символах. Запускай через `ETL/controller.py`, там настроен безопасный захват логов.

2.  **Ошибка `default.dim_location does not exist`**:
    *   Значит, Spark пытается писать в стандартную базу. Проверь, что в `process_data_spark.py` в SQL запросах стоит префикс `weather_db.`.

3.  **Ошибка `Java Gateway`**:
    *   Проверь версию Java (`java -version`). Должна быть 17.

4.  **Пути к файлам**:
    *   Желательно, чтобы в пути к проекту **не было кириллицы** (папок типа "Универ"). Spark и NetCDF библиотеки этого не любят.
